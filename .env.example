POSTGRES_USER=pyhr
POSTGRES_PASSWORD=change_me
POSTGRES_DB=pyhr
JWT_SECRET=change_me_in_production
JWT_EXPIRE_MINUTES=60

# Docker Compose host ports (API/Fe still listen on 8000/5173 inside containers).
API_HOST_PORT=8600
FE_HOST_PORT=5180
# Browser → API when using compose (must match API_HOST_PORT on the host).
VITE_API_URL=http://localhost:8600
